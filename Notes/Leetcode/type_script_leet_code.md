1. array of array with fix length	const graph: number[][] = Array.from({length:numCourses},()=>[]);
2. swap variable	[a,b]=[b,a]
3. pop right	stack.pop()!
4. array fix length filled 0	const ans = Array(temperatures.length).fill(0);
5. if l null, return r, else return l	return l??r
6. if l null or l.val null	let lval =l?.val?? Infinity
7. dictionary (rec, key=str/num/sym	d: Record<string,any>;
8. dictionary (map, key=any	map: Map<number, ListNode>;
9. for loop	for (const a of b)
10. check if key in dict	if (key in d)
11. array popleft	queue.shift()!
12. define function (arrow)	const dfs = (i:number, j: number):void=> {...}
13. define function ( normal, hoisted	function merge(l1: ListNode | null, l2: ListNode | null): ListNode | null {
14. maximum	Math.max(num1,num2,num3,num4)
15. class definition	"class ListNode {
    val:number
    key: number
    next: ListNode|null
    prev: ListNode|null
    constructor(key:number,val:number){
        this.key=key
        this.val=val
        this.prev=null
        this.next=null
    }
}"
16. create of new object	const node = new ListNode(1, 10);
17. get substring, slice(a) means from a to end	s.slice(a,b)
18. create set	const set = new Set<number>(nums);
19. check if in set	set.has(xxx)
	set.add(xxx)
	set.delete(xxx)
20. sum array	nums.reduce((acc,cur)=>acc+cur,0)
21. absolute	Math.abs(x)
22. inifinity	let x=Infinity